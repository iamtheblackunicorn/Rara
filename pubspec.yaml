name: rara
description: The old challenge of the guessing game, revisited!
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any
flutter:
  generate: true
  uses-material-design: true
  fonts:
    - family: LSD
      fonts:
        - asset: assets/fonts/LSD.ttf
